# NeighborTools Production Environment Configuration
# Copy this file to .env on your Hetzner VM and fill in the values

# ===========================================
# Database Configuration
# IMPORTANT: Use strong passwords in production!
# ===========================================
POSTGRES_USER=neighbortools
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD
POSTGRES_DB=neighbortools

# ===========================================
# JWT Configuration
# IMPORTANT: Generate a strong secret!
# Generate with: openssl rand -base64 64
# ===========================================
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_SECRET

# ===========================================
# SMTP Configuration (for email notifications)
# ===========================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@neighbortools.net
